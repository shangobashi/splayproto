# iOS (SwiftUI) — Splay

Open `Splay.xcodeproj` in Xcode.

## Configure API base
Default API base is `http://localhost:8000`.

If you run the API elsewhere, set an environment variable in the Xcode scheme:
- `API_BASE` = `http://<your-ip>:8000`

Then Build & Run on simulator.
